{
  "version": "1.0",
  "generatedAt": "2026-09-04T11:37:00.000Z",
  "integrations": {
    "core": {
      "tools": [
        {
          "name": "clickElement",
          "description": "Click an element on the page. Identify it by a `ref` handle from findElements (preferred) or a CSS `selector`; pass at least one. Errors if neither is given or the element cannot be found.",
          "inputSchema": {
            "properties": {
              "ref": {
                "description": "A handle from findElements (e.g. \"el-12\"). Preferred \u2014 pass this when you have one.",
                "minLength": 1,
                "type": "string"
              },
              "selector": {
                "description": "A CSS selector (e.g. \"#add-to-cart\"). Use only when you do not have a handle. First match is clicked.",
                "minLength": 1,
                "type": "string"
              }
            },
            "type": "object"
          }
        },
        {
          "name": "describeElement",
          "description": "Return detailed information about one element, identified by a `ref` handle from findElements (preferred) or a CSS `selector`; pass at least one. Includes its accessible name and how it was derived, role, all attributes, visibility and interaction state, position, the region it belongs to, its associated label, nearby interactive controls, and a summary of its children. Use this to confirm you have the right element before acting on it.",
          "inputSchema": {
            "properties": {
              "ref": {
                "description": "A handle from findElements (e.g. \"el-12\"). Preferred \u2014 pass this when you have one.",
                "minLength": 1,
                "type": "string"
              },
              "selector": {
                "description": "A CSS selector. Use only when you do not have a handle.",
                "minLength": 1,
                "type": "string"
              }
            },
            "type": "object"
          },
          "annotations": {
            "readOnlyHint": true
          }
        },
        {
          "name": "fillInput",
          "description": "Enter a value into a text input, textarea, or contenteditable element. Identify the element by a `ref` handle from findElements (preferred) or a CSS `selector`; pass at least one. For checkboxes, radios, and buttons use clickElement instead.",
          "inputSchema": {
            "properties": {
              "ref": {
                "description": "A handle from findElements (e.g. \"el-12\"). Preferred \u2014 pass this when you have one.",
                "minLength": 1,
                "type": "string"
              },
              "selector": {
                "description": "A CSS selector. Use only when you do not have a handle.",
                "minLength": 1,
                "type": "string"
              },
              "value": {
                "description": "The text to enter. An empty string clears the field.",
                "type": "string"
              }
            },
            "required": [
              "value"
            ],
            "type": "object"
          }
        },
        {
          "name": "findElements",
          "description": "Search the current page for elements matching any combination of criteria, and return a ranked list of compact descriptors \u2014 each with a `ref` handle you can pass to scrollToElement, highlightElement, or clickElement. Combine criteria to narrow results (e.g. category \"input\" + text \"promo\"). Results are visible elements by default, ranked by how well they match \u2014 NOT by where they sit on screen. Each result reports whether it is currently in view (state.inViewport); when the element you want is not in view, call scrollToElement with its handle to bring it on-screen before highlighting or clicking. Prefer this over guessing CSS selectors.",
          "inputSchema": {
            "properties": {
              "attributes": {
                "description": "Match attributes by substring, e.g. {\"placeholder\":\"code\",\"type\":\"text\"}. An empty value matches presence only.",
                "type": "object"
              },
              "category": {
                "description": "High-level element bucket. \"clickable\" also surfaces heuristically-detected click targets (divs/spans with click affordances).",
                "enum": [
                  "input",
                  "button",
                  "link",
                  "image",
                  "heading",
                  "landmark",
                  "media",
                  "control",
                  "clickable"
                ],
                "type": "string"
              },
              "inViewportOnly": {
                "description": "Only elements currently in the viewport. Default false.",
                "type": "boolean"
              },
              "limit": {
                "description": "Max results (1\u201350). Default 20.",
                "type": "number"
              },
              "matchMode": {
                "description": "How `text` matches. Default \"fuzzy\" (lexical recall). Use \"exact\" once you know the literal on-page string.",
                "enum": [
                  "fuzzy",
                  "exact"
                ],
                "type": "string"
              },
              "near": {
                "description": "A handle (from a prior findElements) \u2014 restrict to elements spatially or structurally near it.",
                "type": "string"
              },
              "offset": {
                "description": "Skip the first N matches (paging). Default 0.",
                "type": "number"
              },
              "role": {
                "description": "ARIA role: button, link, textbox, checkbox, radio, img, heading, etc.",
                "type": "string"
              },
              "text": {
                "description": "Free-text match over the element's whole textual surface \u2014 accessible name, own text, placeholder, aria-label, alt, title, value, and the associated <label>. Lexical and recall-oriented: case/accent-insensitive, word-order-independent, partial words allowed. Try synonyms across separate calls (e.g. \"discount\" then \"promo\"); this does NOT expand synonyms for you.",
                "type": "string"
              },
              "visibleOnly": {
                "description": "Only visible elements. Default true.",
                "type": "boolean"
              },
              "within": {
                "description": "A handle, or a landmark/section name (e.g. \"Order Summary\") \u2014 restrict the search to that subtree.",
                "type": "string"
              }
            },
            "required": [],
            "type": "object"
          },
          "annotations": {
            "readOnlyHint": true
          }
        },
        {
          "name": "getPageContext",
          "description": "Return metadata about the current page: url, canonicalUrl, title, meta tags (description, keywords, Open Graph, Twitter Card, etc.), and parsed JSON-LD structured-data blocks. Use this to understand what page the user is on.",
          "inputSchema": {
            "properties": {},
            "required": [],
            "type": "object"
          },
          "annotations": {
            "readOnlyHint": true
          }
        },
        {
          "name": "getPageOutline",
          "description": "Return the structural skeleton of the current page: landmark regions (header, nav, main, footer, and labeled sections), the heading outline, the forms present, and a count of interactive elements by category. Use this first to understand how the page is organized before searching for specific elements with findElements. Landmarks and forms include a handle you can pass to findElements' `within` to scope a search.",
          "inputSchema": {
            "properties": {},
            "required": [],
            "type": "object"
          },
          "annotations": {
            "readOnlyHint": true
          }
        },
        {
          "name": "highlightElement",
          "description": "Draw the user's attention to an element on the page (a temporary visual emphasis). Identify it by a `ref` handle from findElements (preferred) or a CSS `selector`; pass at least one. Errors if neither is given or the element cannot be found.",
          "inputSchema": {
            "properties": {
              "ref": {
                "description": "A handle from findElements (e.g. \"el-12\"). Preferred \u2014 pass this when you have one.",
                "minLength": 1,
                "type": "string"
              },
              "selector": {
                "description": "A CSS selector (e.g. \"#promo-banner\"). Use only when you do not have a handle. First match is highlighted.",
                "minLength": 1,
                "type": "string"
              }
            },
            "type": "object"
          }
        },
        {
          "name": "navigateToUrl",
          "description": "Navigate the current page to a URL on the same site. Accepts an absolute URL or a path (e.g. \"/cart\"). Same-origin only \u2014 cross-origin navigation is rejected. If the URL already matches the current page, navigation is skipped. Returns the URLs navigated from and to, plus a `navigated` flag (false when skipped, with a `reason` explaining why).",
          "inputSchema": {
            "properties": {
              "url": {
                "description": "An absolute same-origin URL (e.g. \"https://shop.example.com/cart\") or a path (e.g. \"/cart\").",
                "minLength": 1,
                "type": "string"
              }
            },
            "required": [
              "url"
            ],
            "type": "object"
          }
        },
        {
          "name": "scrollToElement",
          "description": "Scroll an element into view. Identify it by a `ref` handle from findElements (preferred) or a CSS `selector`; pass at least one. Use this when a findElements result reports state.inViewport: false, to bring it on-screen before highlighting or clicking. Errors if neither is given or the element cannot be found.",
          "inputSchema": {
            "properties": {
              "ref": {
                "description": "A handle from findElements (e.g. \"el-12\"). Preferred \u2014 pass this when you have one.",
                "minLength": 1,
                "type": "string"
              },
              "selector": {
                "description": "A CSS selector (e.g. \"#reviews\"). Use only when you do not have a handle. First match is scrolled into view.",
                "minLength": 1,
                "type": "string"
              }
            },
            "type": "object"
          }
        }
      ]
    },
    "common": [
      {
        "name": "bazaarvoice",
        "config": {
          "clientName": "Macys",
          "locale": "en_US",
          "bfdToken": "7129,main_site,en_US"
        },
        "tools": [
          {
            "name": "getProductReviews",
            "description": "Get customer reviews for a product from Bazaarvoice. Supports pagination, sorting, filtering by star rating, keyword search, sorting by a product feature, and a photos-only mode. Each review carries title, body, rating, date, recommendation flag, helpfulness votes, and photo URLs.",
            "inputSchema": {
              "type": "object",
              "properties": {
                "productId": {
                  "type": "string",
                  "description": "The id of the product. Required",
                  "minLength": 1
                },
                "limit": {
                  "type": "integer",
                  "description": "Number of reviews to return (default: 5, max: 10). Capped low because BV review payloads are heavy.",
                  "minimum": 1,
                  "maximum": 10
                },
                "offset": {
                  "type": "integer",
                  "description": "Number of reviews to skip for pagination (default: 0).",
                  "minimum": 0
                },
                "sort": {
                  "type": "string",
                  "description": "Sort order. Ignored when `search` is supplied (Bazaarvoice sorts by relevance during search and returns 0 results if both are sent).",
                  "enum": [
                    "rating:desc",
                    "rating:asc",
                    "submissiontime:desc",
                    "submissiontime:asc",
                    "helpfulness:desc"
                  ]
                },
                "ratingFilter": {
                  "type": "integer",
                  "description": "Filter to reviews with this exact star rating (1-5).",
                  "minimum": 1,
                  "maximum": 5
                },
                "search": {
                  "type": "string",
                  "description": "Keyword search within review text (e.g. \"shipping\", \"quality\"). Bazaarvoice ranks results by relevance; the `sort` argument is ignored when this is set."
                },
                "feature": {
                  "type": "string",
                  "description": "Sort reviews by relevance to a named product feature/attribute (e.g. \"satisfaction\", \"quality\", \"value\")."
                },
                "photosOnly": {
                  "type": "boolean",
                  "description": "When true, returns only reviews that include photos."
                }
              },
              "required": [
                "productId"
              ]
            }
          },
          {
            "name": "getReviewSummary",
            "description": "Get Bazaarvoice's aggregate review summary for a product. Returns the raw BV summary envelope (average rating, total review count, rating distribution, and bullet-point highlights).",
            "inputSchema": {
              "type": "object",
              "properties": {
                "productId": {
                  "type": "string",
                  "description": "The id of the product. Required",
                  "minLength": 1
                }
              },
              "required": [
                "productId"
              ]
            }
          },
          {
            "name": "getProductQuestions",
            "description": "Get customer Q&A (questions and answers) for a product from Bazaarvoice. Use `search` to check whether a shopper's question has already been asked and answered. Each question carries its text, author, date, and answer count, with its answers inline \u2014 answer text, author, date, and an `isBrandAnswer` flag marking official brand/staff replies.",
            "inputSchema": {
              "type": "object",
              "properties": {
                "productId": {
                  "type": "string",
                  "description": "The id of the product. Required",
                  "minLength": 1
                },
                "limit": {
                  "type": "integer",
                  "description": "Number of questions to return (default: 5, max: 10).",
                  "minimum": 1,
                  "maximum": 10
                },
                "offset": {
                  "type": "integer",
                  "description": "Number of questions to skip for pagination (default: 0).",
                  "minimum": 0
                },
                "sort": {
                  "type": "string",
                  "description": "Sort order. Defaults to Bazaarvoice relevance when omitted. Unlike reviews, `sort` and `search` can be combined.",
                  "enum": [
                    "submissiontime:desc",
                    "submissiontime:asc",
                    "totalanswercount:desc",
                    "lastapprovedanswersubmissiontime:desc"
                  ]
                },
                "search": {
                  "type": "string",
                  "description": "Keyword search within question text (e.g. \"storage\", \"lights\", \"assembly\"). Returns the questions most relevant to the term."
                }
              },
              "required": [
                "productId"
              ]
            }
          }
        ]
      }
    ],
    "custom": []
  }
}